"use client";

import ResetPassword from "@/widget/chanciAI/ResetPassword";
import { Suspense } from "react";

const Page = () => {


    return (
        <Suspense>
            <ResetPassword />
        </Suspense>
    );
};

export default Page;